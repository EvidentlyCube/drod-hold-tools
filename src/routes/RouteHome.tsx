import FullPageMessage from "../components/common/FullPageMessage";

export default function RouteHome() {
	return (
		<FullPageMessage header="DROD Hold Tools">
			<p>
				Use <em>Add Hold</em> button in the top-right to
				<br />
				load a hold for processing.
			</p>
		</FullPageMessage>
	);
}
